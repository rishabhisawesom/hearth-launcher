# Contributing

- Use conventional commits (`feat:`, `fix:`, `docs:`, `chore:`)
- Reference Linear issues in branches and PRs (`RIS-XXX`)
- Keep PRs focused; include screenshots for UI changes

## Branches

- `main` — stable
- `feature/ris-XXX-short-description` — feature work
